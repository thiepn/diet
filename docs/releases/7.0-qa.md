# Diet Copilot V7.0 — QA / Certification Matrix

## Automated certification

- [x] Web 1.0 nutrition/coaching regressions pass
- [x] Web bundle generation remains deterministic
- [x] actual GitHub Pages/Jekyll build passes
- [x] Android project is generated from maintained sources
- [x] Android minSdk is 26 and target/compile SDK is compatible with Health Connect
- [x] Health Connect Kotlin sources compile
- [x] Capacitor custom plugin compiles and is registered
- [x] WorkManager background worker compiles
- [x] Android Keystore session storage compiles
- [x] native reminder workers compile
- [x] debug APK is produced and uploaded by GitHub Actions
- [x] browser remains nutrition-write-disabled
- [x] canonical Supabase project remains the only active backend
- [x] activity-calorie eat-back remains disabled

## Backend checks

- [x] `public.diet_native_devices` has RLS enabled
- [x] authenticated access to `diet_native_devices` is owner-scoped SELECT only
- [x] authenticated access to `activity_daily` remains SELECT only
- [x] `activity_daily` has only the owner SELECT RLS policy for authenticated browser sessions
- [x] `diet-health-sync` is deployed with JWT verification enabled
- [x] no Health Connect test activity rows remain in production
- [x] no native device test rows remain in production
- [x] security advisor reports no new Diet V7-specific finding

## Physical-device certification still required

- [ ] install current debug APK on an eligible Android phone
- [ ] email/password login in native shell
- [ ] Health Connect permission screen opens correctly
- [ ] steps read correctly from Health Connect
- [ ] active calories read correctly
- [ ] distance reads correctly
- [ ] exercise sessions produce reasonable exercise-minute totals
- [ ] foreground Sync now writes canonical `activity_daily`
- [ ] Today/Insights refresh after native activity sync
- [ ] background sync runs after the app is closed
- [ ] background sync survives phone reboot / OEM battery management as expected
- [ ] revoking Health Connect permission degrades cleanly
- [ ] offline launch remains usable with cached web state
- [ ] session refresh works after access-token expiry
- [ ] weigh-in notification fires at configured local time
- [ ] weekly-review notification fires at configured local time
- [ ] notification denial does not break the app
- [ ] Android back navigation behaves correctly across tabs/dialogs
- [ ] 320–480 px phone layouts remain overflow-free in native WebView

## Google sign-in certification still required

- [ ] register/allow the Android callback/deep-link in Supabase Auth
- [ ] use system-browser OAuth rather than an embedded Google user agent where required
- [ ] verify callback returns to `dev.thiepn.diet`
- [ ] verify THIEPN Account session is handed to native encrypted storage

## Production release prerequisites

- [ ] create user-owned Android release keystore
- [ ] configure CI/local signing without committing secrets
- [ ] build signed release AAB
- [ ] complete Play Console app metadata and Health Connect declarations
- [ ] run final signed-build device test

V7.0 is considered code-complete when automated certification is green. It is considered device-certified only after the physical-device section passes, and Play-ready only after the production-release prerequisites pass.
