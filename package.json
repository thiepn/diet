{
  "name": "diet-copilot",
  "version": "7.0.0",
  "private": true,
  "description": "Diet Copilot web dashboard and native Android companion",
  "scripts": {
    "build:web": "node scripts/build-v68.mjs",
    "build:native-web": "node scripts/build-native-web.mjs",
    "android:prepare": "node scripts/prepare-android.mjs",
    "android:build": "npm run android:prepare && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "8.4.2",
    "@capacitor/core": "8.4.2",
    "@supabase/supabase-js": "2.116.0"
  },
  "devDependencies": {
    "@capacitor/cli": "8.4.2"
  },
  "engines": {
    "node": ">=22"
  }
}
